"""Automation Catalog bounded context.

The library of approved, reusable building blocks operators may run: unified runner templates
across all engines, their survey-field schemas, ownership, and approval state. Engineers author
and approve building blocks; operators browse and execute the approved ones (governed
self-service). Executing a template maps survey answers to a connector ExecutionRequest and
hands off to the execution engine.
"""
